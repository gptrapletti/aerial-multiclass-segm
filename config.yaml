# Datamodule
data_path: data
images_dir: data/images
masks_dir: data/masks
split_path: data/split.json
n_random_patches_per_image: 8 # for patch = 512, max = 8
patch_size: 512
overlap: 0
train_batch_size: 6 # for patch = 512, max = 6
val_batch_size: 14
num_workers: 12

# Model and training
lr: 0.0001
epochs: 50


# General
train_split: 250 # 0.625%
val_split: 100 # 0.25%
test_split: 50 # 0.125%

# Run
experiment_name: 'aerial-multiclass-seg'
run_description: 'patch-size:512_smp-activation:none-truly'

# loss_fn:
#   _target_: torch.nn.BCELoss